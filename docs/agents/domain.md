# Domain Docs

Before exploring the codebase, read relevant root `CONTEXT.md` and `docs/adr/` files when they exist. If they do not exist, proceed silently.

## Layout

This is a single-context repository:

/
├── CONTEXT.md
├── docs/adr/
└── src/

Use terminology defined in `CONTEXT.md`. Explicitly flag conflicts with relevant ADRs.
